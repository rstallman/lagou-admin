  
  {{each data}}
  <tr>
    <td>{{$index+1}}.</td>
    <td>{{$value.username}}</td> 
    <td><button data-id="{{$value._id}}" class="btn btn-danger remove" data-toggle="modal" data-target="#removeModal">删除</button></td>
  </tr>
  {{/each}}